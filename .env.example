# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# =============================================================================
# SECURITY WARNING: 
# - Only use NEXT_PUBLIC_ for non-sensitive configuration that can be exposed to clients
# - All API keys, secrets, and credentials should be server-side only (no NEXT_PUBLIC_ prefix)
# =============================================================================

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=https://localhost:3000
NEXT_PUBLIC_APP_VERSION=1.0.0

# API Configuration
NEXT_PUBLIC_API_URL=https://localhost:3001

# Database Configuration (Server-side only)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-key-here

# Email Service Configuration (Server-side only)
MAILJET_API_KEY=your-mailjet-api-key
MAILJET_SECRET_KEY=your-mailjet-secret-key
MAILJET_FROM_EMAIL=noreply@yourcompany.com
MAILJET_FROM_NAME=Your Company Name

# Email Sender Configuration (Server-side only)
SENDER_EMAIL=noreply@yourcompany.com
SENDER_NAME=Your CRM System

# E-signature Service (Server-side only)
BOLDSIGN_API_KEY=your-boldsign-api-key
BOLDSIGN_WEBHOOK_URL=https://yourapp.com/api/webhooks/boldsign

# Payment Processing (Server-side only)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Encryption Keys (Server-side only)
# Generate strong random keys for production:
# openssl rand -hex 32
ENCRYPTION_KEY=generate-strong-32-byte-hex-key-for-production
EMAIL_MASTER_KEY=generate-strong-32-byte-hex-key-for-email-encryption
ENCRYPTION_MASTER_KEY=generate-strong-32-byte-hex-key-for-master-encryption

# Test Configuration (Development/Testing only)
TEST_PASSWORD=secure-test-password-from-env

# Slack Integration (Optional)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# Analytics (Optional)
ANALYZE=false

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST:
# =============================================================================
# □ All encryption keys are generated using cryptographically secure methods
# □ All API keys are rotated from development/testing values
# □ No NEXT_PUBLIC_ prefixes on sensitive credentials
# □ All required environment variables are set
# □ .env.local is added to .gitignore
# □ Environment variables are stored securely (AWS Secrets Manager, etc.)
# =============================================================================